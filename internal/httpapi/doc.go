// Package httpapi is the HTTP layer: routing, middleware, JSON encoding, and
// the mapping from domain errors to status codes. Handlers depend on service
// interfaces, never on storage.
package httpapi
